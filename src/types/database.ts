// Supabase 자동 생성 타입 (Phase 1.4에서 추가 예정)
// npx supabase gen types typescript --project-id <project-id> --schema public > src/types/database.ts

export type Database = {
  // Supabase CLI로 자동 생성될 예정
};
