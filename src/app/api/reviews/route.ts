import { NextResponse } from "next/server";

// 강의평 관련 API
export async function GET(request: Request) {
  return NextResponse.json({ message: "Reviews API - 구현 예정" });
}

export async function POST(request: Request) {
  return NextResponse.json({ message: "Reviews API - 구현 예정" });
}
