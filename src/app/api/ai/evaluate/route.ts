import { NextResponse } from "next/server";

// AI 평가 API (Phase 6에서 구현)
export async function POST(request: Request) {
  return NextResponse.json({ message: "AI Evaluate API - Phase 6에서 구현 예정" });
}
