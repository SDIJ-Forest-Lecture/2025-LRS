import { NextResponse } from "next/server";

// 배정 관련 API
export async function GET(request: Request) {
  return NextResponse.json({ message: "Assignments API - 구현 예정" });
}

export async function POST(request: Request) {
  return NextResponse.json({ message: "Assignments API - 구현 예정" });
}
