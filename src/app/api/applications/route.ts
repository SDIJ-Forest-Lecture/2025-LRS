import { NextResponse } from "next/server";

// 강의 신청 API
export async function GET(request: Request) {
  return NextResponse.json({ message: "Applications API - 구현 예정" });
}

export async function POST(request: Request) {
  return NextResponse.json({ message: "Applications API - 구현 예정" });
}
